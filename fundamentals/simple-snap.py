# Write a Python script which reads two numbers and outputs the
# message snap! if they are the same (and no message at all if 
# they are not).

#!/usr/bin/env python3

a = int(input())
b = int(input())

if a == b:
  print('snap!')